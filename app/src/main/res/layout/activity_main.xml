<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/guessWordText"
        android:layout_width="470dp"
        android:layout_height="103dp"
        android:layout_marginStart="20dp"
        android:layout_marginTop="900dp"
        android:layout_marginEnd="53dp"
        android:layout_marginBottom="21dp"
        android:inputType="textPersonName"
        android:textSize="30sp"
        android:text="Enter a 4 letter guess here"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/guessEnterButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/guessEnterButton"
        android:layout_width="205dp"
        android:layout_height="90dp"
        android:layout_marginBottom="28dp"
        android:text="Guess"
        android:textSize="30sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.728"
        app:layout_constraintStart_toEndOf="@+id/guessWordText" />

</androidx.constraintlayout.widget.ConstraintLayout>